/** Code-facing namespace. User-facing strings use `project-star` instead. */
export const ID_NAMESPACE = 'ps';

/** Source identity: which JSX node in which file produced this DOM node. */
export const ID_ATTR = 'data-ps-id';

/** Runtime instance identity, for nodes rendered N times from one source node. */
export const KEY_ATTR = 'data-ps-key';